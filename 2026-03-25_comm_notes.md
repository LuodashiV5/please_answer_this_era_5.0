# 2026-03-25 沟通纪要（Codex）

> 日期：2026-03-25  
> 项目：MVS-CPU2-Project / HandCode_20250412_V145

## 1. 原理图与 RS-485 是否实现

### 你的问题
- 原理图里有 RS-485，代码里是否已实现 RS-485 通讯？

### 结论
- **硬件有 RS-485 电路**（含 `SP3485EEN`、`RS485_EN`、`RS485_TXD`、`RS485_RXD`、`SCIRXDA/SCITXDA`、`GPIO133`）。
- **当前 CPU2 应用代码没有实现 RS-485 通讯链路**（未见 RS-485 方向控制与 RTU 串口收发状态机）。
- 当前通信主链路是 **W5500 + Modbus/TCP**。

### 关键依据（已核对）
- 协议解析使用 MBAP 头（TCP 报文格式），不是 RTU 帧。
- 代码主流程在跑 W5500 Socket 状态机。
- CPU2 工程构建清单未编译 SCI 通信实现文件作为应用链路。

---

## 2. RS-485 电路是否“在 CPU2”

### 你的问题
- RS-485 电路是不是确定挂在 CPU2？

### 结论
- 在 F28377D 上，`SCIA + GPIO133` 的归属是**软件可分配**，不是硬件永久绑死。
- 即：
  - `SCIA` 看 `CPUSEL5.SCI_A`
  - `GPIO133` 看 `GPECSEL1.GPIO133`
- 你当前工程里，没有看到一条完整、明确的“RS-485 应用链路已在 CPU2 正式启用”的实现闭环（尤其 `GPIO133` 方向控制没有在应用层落地）。

### 给新手的实用判断
- 想让 CPU2 管 RS-485，必须同时满足：
  1. `SCIA` 分配给 CPU2；
  2. `GPIO133` 也分配给 CPU2；
  3. CPU2 应用里有 DE/RE 时序控制 + 串口收发状态机。

---

## 3. RS-485 速率上限与价值定位

### 你的问题
- 既然 CPU1 做高速计算，这路 RS-485 理论能多快？还有没有价值？

### 结论
- 物理层收发器 `SP3485` 规格上限可到 **10 Mbps**（器件级）。
- 结合该工程常见时钟配置，SCI 侧理论可达数 Mbps 级（约 **6.25 Mbps** 量级上限估算）。
- 但工程实际取决于线长、EMC、终端匹配、协议开销，稳定常用通常远低于理论峰值。

### 价值判断
- **高速采集主链路**：建议 Ethernet（W5500 / TCP）。
- **RS-485**：适合作为控制、参数、状态、故障码、维护/保底链路，仍有工程价值。

---

## 4. Modbus Poll（TCP）采样频率问题

### 你的问题
- Modbus Poll TCP 能改采样频率吗？最小只能 1ms（1kHz）吗？

### 结论
- 可以改轮询周期（Scan Rate），但这是**轮询间隔**，不是设备内部 ADC 采样率。
- 新版文档中可设到 `0 ms`（尽快轮询）；旧资料常见 `1 ms` 起。
- 设成 1ms 或 0ms 不代表一定达到 1kHz，实际受事务时延、从站处理、OS 调度、网络栈影响。

---

## 5. 是否推荐移植 nanoMODBUS

### 你的目标
- 提升扩展性、把黑匣子做深、支持后续数据读出与点位扩展。

### 结论
- **推荐**移植 nanoMODBUS。
- 该协议栈可支持 **RTU + TCP**，也就是你要的“RS-485 与 TCP 共存”。
- 工程实现建议：
  - 建两个实例（RTU 一个、TCP 一个）；
  - 共用同一套寄存器/黑匣子数据模型；
  - 保持地址映射兼容，避免上位机断裂。

### 重要说明
- 不是“一个实例同时跑两种传输”，而是“同一协议栈代码，两个实例分别绑定不同底层收发”。

---

## 6. 聊天/输出如何本地保存

### 你的问题
- 怎么把输出保存到本地？PowerShell 怎么用？之前记录能补吗？

### 结论
- 记录建议使用：
  - `Start-Transcript ...`
  - `Stop-Transcript`
- Transcript **不能回溯**，只能记录开启之后的内容。
- 之前未记录时，可查 PowerShell 历史命令（仅命令，不含完整交互输出）。

---

## 7. 后续执行建议（明天上班可直接接）

1. 先定通信架构：
- Ethernet 为高速主链路；
- RS-485 为控制/维护副链路。

2. 再定归属策略：
- 确认 `SCIA` 与 `GPIO133` 统一归 CPU2（或统一归 CPU1），避免跨核割裂。

3. 开始协议改造：
- 引入 nanoMODBUS；
- 先做最小双实例骨架（RTU+TCP）；
- 共用寄存器映射与黑匣子读取接口。

4. 验证计划：
- RTU 回环与主站轮询压测；
- TCP 并发读写压测；
- 黑匣子分页读取一致性测试。

---

## 8. 一句话总括

- 你现在的代码是 **Modbus/TCP 主链路已跑通**，而 **RS-485 硬件在板上但应用层未真正启用**；
- 下一步最优解是 **nanoMODBUS 双通道（RTU+TCP）共用数据模型**，既保扩展性，也不牺牲高速链路。
