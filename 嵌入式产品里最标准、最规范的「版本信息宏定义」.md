
用于开机打印、串口输出、固件标识、产品溯源。

头文件宏定义（不变，干净简洁)

```c
#define APP_NAME        "网线版"
#define APP_DATE        "260426"
#define APP_VERSION      "1.0"
#define PCB_VERSION      "02.011.03 V1.1"
#define APP_AUTHOR       "RED-POTATO"
// 自动拼接版本字符串（标准写法）
#define VER_MSG    "VER=" APP_NAME " " APP_DATE " v" APP_VERSION "!\r\n" PCB_VERSION "\r\n"
#define START_MSG   " " APP_NAME " " APP_DATE " by " APP_AUTHOR " \r\n"
```

长度用 **编译期计算**

```c
// 编译期计算长度（比 strlen 安全100倍）
#define VER_MSG_LEN      (sizeof(VER_MSG) - 1)
#define START_MSG_LEN    (sizeof(START_MSG) - 1)
```

实际使用：

封装成 **static 私有版本信息结构体**

```c
// 版本结构体
typedef struct {
    const char* ver_msg;
    uint16_t ver_len;
    const char* start_msg;
    uint16_t start_len;
} AppVersion_t;
```

```c
// 静态私有
static const AppVersion_t s_app_ver = {
    .ver_msg    = VER_MSG,
    .ver_len    = VER_MSG_LEN,
    .start_msg  = START_MSG,
    .start_len  = START_MSG_LEN,
};
```

**对外接口函数**

```c
// 获取版本信息（出参：msg + len）
void app_get_version(const char** msg, uint16_t* len)
{
    // F 防死机
    if (msg == NULL || len == NULL) {
        return;
    }
    *msg = s_app_ver.ver_msg;
    *len = s_app_ver.ver_len;
}
// 获取开机欢迎信息
void app_get_startup(const char** msg, uint16_t* len)
{
    if (msg == NULL || len == NULL) {
        return;
    }
    *msg = s_app_ver.start_msg;
    *len = s_app_ver.start_len;
}
```

**实际输出：**

```c
VER=网线版 260426 v1.0!
02.011.03 V1.1 
网线版 260426 by RED-POTATO
```