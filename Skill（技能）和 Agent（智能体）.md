
在 AI 和大模型应用语境里，Skill（技能）和 Agent（智能体）常被一起提到，但层级和作用不同，可以这样理解：

1) Agent（智能体）

Agent 更像“一个能自主完成任务的角色/系统”。  
典型特征：
• 有目标或任务（例如：“帮我部署服务”“做数据分析”）

• 可以做决策：下一步做什么、用什么工具、什么时候停止

• 通常能使用工具/插件（搜索、代码执行、数据库、API 等）

-可能具备记忆（上下文、对话历史、长期记忆）
• 例子：一个“运维 Agent”可在你给出目标后，自己查日志、分析、执行命令、反馈结果。

一句话：Agent = 具有目标、决策能力和工具使用能力的执行者。

2) Skill（技能）

Skill 是“某个具体能力/工具调用单元”，通常被 Agent 调用。  
典型特征：
• 功能单一、明确：做一件具体的事

• 往往是工具、函数、API、提示模板的封装

- 一般不包含复杂自主决策（或只做很小的决策）
• 例子：  

  • web_search(query)  

  • run_sql(sql)  

  • send_email(to, subject, body)  

  - code_interpreter(code)

一句话：Skill = Agent 可使用的“本领/工具函数”。

3) 两者关系（很关键）

• Agent 可以拥有多个 Skill，并在过程中选择合适的 Skill 来用。

- Skill 本身通常不是 Agent：它一般不会自己决定“我要不要做、接下来做什么”，而是被调用。
• 有些系统里也会看到“meta-agent / planner + worker-agents”，这时每个 worker-agent 也可能再组合多个 skill。

类比（不完美但直观）：
• Agent 像“员工/同事”

• Skill 像“员工会用的一件工具或一项具体操作”（比如用搜索引擎、发邮件、跑 SQL）

如果你说的是某个框架（例如微软语义内核、langgraph、coze、dify、autoGPT 类项目），告诉我具体框架，我可以按它的定义把区别说到更精确。


>[!NOTE] Claudecode 能帮人写代码的是 agent 还是skill

帮你写代码的 Claude Code 本体是 Agent（智能体）。

在 Claude Code 的架构里，两者的分工非常清晰：
•   Agent（智能体）是“干活的人”：Claude Code 本身就是一个拥有本地环境操作权（读文件、改代码、跑命令、查 git 等）的 AI Agent。是你下发“帮我写个登录模块”这个目标后，去理解、规划步骤、调用工具并执行写入代码的主体。

*   Skill（技能）是“专业手册/工作流”：Skill 是按需加载的 Markdown 文档或指令模板（比如团队的代码规范、部署流程 /deploy、API 文档等）。Agent 在写代码时，会去查阅相关的 Skill 来获取“应该怎么写”“要符合什么标准”的知识，但它自己不主动去写文件。

简单类比：Claude Code 是程序员（Agent），Skill 是程序员手边翻看的编程规范手册或最佳实践文档。

>[! note]  除了Claude Code，还有哪些典型的AI编程Agent例子，它们各自有什么特点？

目前市面上典型的 AI 编程 Agent（具备自主规划、多步执行、环境交互能力的编码助手）主要有以下几类，各自特点如下：

1. Cursor (Agent Mode / Composer)

*   特点：深度集成在 IDE（VS Code 分支）中，可能是目前最流行的“半自动”编码 Agent。
•   核心能力：

    ◦   Composer (Agent 模式)：可以跨多个文件进行编辑，不仅能写新代码，还能根据指令修改现有逻辑、创建组件。

    *   上下文感知极强：直接读取当前项目结构、打开的文件、终端报错，无需频繁复制粘贴。
    ◦   自动修复：能根据终端报错自动尝试修复代码。

•   定位：面向日常开发者，追求“人机协同”编程效率。

2. GitHub Copilot Workspace (或未来的 Agent 模式)

•   特点：由 GitHub 推出，强调从 Issue 到代码的全流程。

•   核心能力：

    *   任务规划：将 GitHub Issue 描述为任务，自动拆解步骤。
    ◦   环境集成：深度绑定仓库、PR、CI 流程。

    ◦   协作：侧重于团队开发流程中的自动化（理解需求 -> 编写 -> 提交 PR）。

*   定位：偏向项目管理和全流程自动化，而非仅仅补全代码。

3. Devin (Cognition AI)

•   特点：被称为“第一个全自主 AI 软件工程师”，强调端-to-end 的独立完成任务能力。

*   核心能力：
    ◦   完全自主：给定目标（如“部署一个网站并修复这些 bug”），它能自己在云环境中打开终端、查文档、写代码、调试、部署。

    ◦   长链条推理：能处理非常复杂的、耗时长的任务，不需要人盯着每一步。

    *   沙boxed 环境：拥有自己的浏览器和 shell。
•   定位：替代初级工程师执行完整任务（目前主要对企业开放）。

4. OpenDevin (开源社区项目)

*   特点：开源版的 Devin，社区驱动。
•   核心能力：

    ◦   可自托管：可以在本地或私有云运行，保护代码隐私。

    ◦   插件化：支持多种 LLM 后端（Claude, GPT-4o, 本地模型）。

    *   交互式 Agent：提供 Web UI，允许人类在 Agent 执行步骤时进行干预和指导。
•   定位：给想要自建或定制 AI 工程师的团队/个人。

5. Aider

*   特点：终端（CLI）优先的编码 Agent，极客风格。
•   核心能力：

    ◦   Repo 地图：对大型代码库有更好的结构化理解。

    ◦   双向同步：在终端对话修改代码，同时直接应用到本地文件。

    *   强可控性：通过命令行参数精细控制哪些文件可被修改，适合脚本化工作流。
•   定位：喜欢终端/Vim/Emacs 的开发者，或想集成到 CI 流程中的场景。

6. Windsurf (Codeium)

*   特点：Cursor 的主要竞争对手，强调“Flows”（流）。
•   核心能力：

    ◦   Cascade 引擎：不仅预测下一个编辑，还能理解你正在做的多步操作，主动建议下一步。

    *   深度上下文：能感知运行时的状态（如前端预览效果）。
•   定位：类似 Cursor，注重实时代码理解和多文件编辑体验。

总结对比：

•   集成度最高（IDE内）：Cursor, Windsurf

*   最自主（全闭环）：Devin, OpenDevin
•   流程导向（Issue->PR）：GitHub Copilot Workspace

•   CLI/极客向：Aider, Claude Code

这些工具大多都符合我们之前说的 Agent 定义：有明确目标、能使用工具（文件系统、终端、浏览器）、能做多步决策。而它们内置的“代码规范”、“API 知识”则属于 Skill 范畴。

