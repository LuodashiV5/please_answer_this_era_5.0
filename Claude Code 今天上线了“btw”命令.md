Claude Code 今天上线了“/btw”命令，也就是英文里“by the way”的缩写，可以让你的 CC 在干活儿的同时，不打断当前任务问它与当前任务相关的话题。以前得干等着它干完这个任务才可以。  
  
核心特点：  
  
1. 侧链对话，不打断主任务  
用 btw 开头发起一个提问时，Claude 会在独立线程里回答，主任务继续跑，不受影响。  
  
2. 只读模式  
btw 对话运行在只读模式下，不会触发任何工具调用。  
  
3. 不占用主对话历史  
btw 问答不会写入主会话的上下文，不会占用 context window 空间，也不会干扰主任务的上下文连贯性。  
  
4. 使用方式  
在 Claude Code 执行任务的过程中，直接输入以 btw 开头的消息即可。  
btw 这个 API 的返回值格式是什么？  
  
一句话总结：  
  
btw = 一个临时的、只读的、用完即弃的快问快答窗口，专门给主任务跑着时脑子里冒出来的小问题用的。

![[9ce11b40e003f5671b8a33440e6afe78.png|695]]