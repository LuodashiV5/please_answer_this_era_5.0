

>掌握 Claude Code CLI 的 18 个内置工具，重新定义你的命令行开发体验

![Claude Code 工具全景图](https://mmbiz.qpic.cn/sz_mmbiz_png/cicSL71dNl8iceLtVcjbBMJzbfStdpa4QlDibiawwPAbtUhlia9vnXK8QhgkpqZnJ2QHyxCYzD6jOzruUDmuDkibETiaP4ibMfyic4mht36CkibAWNz9k/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1&watermark=1#imgIndex=0)

Claude Code 工具全景图

---
## 一、TL;DR
- **18 个内置工具**：文件操作(6) + 执行任务(2) + 交互规划(4) + 任务管理(4) + 团队协作(3) + 其他(2)
- **核心工具**：Read、Edit、Bash、Glob、Grep —— 覆盖 80% 日常场景
- **权限配置**：4 种模式，从宽松到严格
- **适合分享**：已优化 SEO 和社交媒体文案

---
## 二、🚀 5 分钟快速上手


>[!场景 1：阅读代码]
你：帮我看看 src/index.ts 的内容   
Claude：使用 Read 工具读取文件...   `

>[!场景 2：修改代码]
>你：把 DEBUG 改成 True   
>Claude：使用 Edit 工具替换...   `

>[!场景 3：搜索代码]
你：找出所有 TODO   
Claude：使用 Grep 工具搜索...   `

---

## 三、📊 工具全景图

| 类别          | 工具                                                  | 核心用途          |
| ----------- | --------------------------------------------------- | ------------- |
| 📁 **文件操作** | Read, Write, Edit, NotebookEdit, Glob, Grep         | 读写、搜索文件       |
| ⚡ **执行任务**  | Bash, Task                                          | 命令执行、Agent 启动 |
| 💬 **交互规划** | AskUserQuestion, EnterPlanMode, ExitPlanMode, Skill | 用户交互、规划模式     |
| 📋 **任务管理** | TaskCreate, TaskGet, TaskUpdate, TaskList           | 创建/查询/更新任务    |
| 👥 **团队协作** | TeamCreate, TeamDelete, SendMessage                 | 多 Agent 协作    |
| 🌐 **其他**   | WebSearch, ToolSearch                               | 网络搜索、工具发现     |

---

## 四、🔧 核心工具（覆盖 80% 场景）

### 4.1 📖 Read - 读取文件

读取本地文件，支持文本、图片、PDF、Jupyter Notebook。

| 参数          | 类型     | 必需  | 说明               |
| ----------- | ------ | --- | ---------------- |
| `file_path` | string | ✅   | 文件的绝对路径          |
| `offset`    | number | ❌   | 起始行号（大文件分段读取）    |
| `limit`     | number | ❌   | 读取行数限制           |
| `pages`     | string | ❌   | PDF 页码，如 `"1-5"` |
>[! 读取完整文件]
>{ "file_path": "/path/to/file.py" }

>[!分段读取大文件   ]
>{ "file_path": "/path/to/large.log", "offset": 100, "limit": 50 }   `

### 4.2 ✂️ Edit - 编辑文件

通过字符串替换编辑文件，必须先使用 Read 读取。

| 参数            | 类型      | 必需  | 说明               |
| ------------- | ------- | --- | ---------------- |
| `file_path`   | string  | ✅   | 文件的绝对路径          |
| `old_string`  | string  | ✅   | 要替换的原始字符串        |
| `new_string`  | string  | ✅   | 替换后的新字符串         |
| `replace_all` | boolean | ❌   | 替换所有匹配（默认 false） |
>[!替换单处]
   {   "file_path": "/path/to/config.py",   "old_string": "DEBUG = False",   "new_string": "DEBUG = True"   }    
   
>[!重命名变量（全部替换） ] 
> {   "file_path": "/path/to/script.py",   "old_string": "old_name",   "new_string": "new_name",   "replace_all": true   }   `

### 4.3 🔍 Glob - 搜索文件

按模式搜索文件路径，比 `find` 更快。

| 参数        | 类型     | 必需  | 说明      |
| --------- | ------ | --- | ------- |
| `pattern` | string | ✅   | glob 模式 |
| `path`    | string | ❌   | 搜索目录    |
|           |        |     |         |
>[!Glob]
>{ "pattern": "src/**/*.tsx" }
>{ "pattern": "**/*.test.js" }   `

### 4.4 🔎 Grep - 搜索内容

基于 ripgrep 的文件内容搜索，支持正则表达式。

| 参数            | 类型      | 必需  | 说明                                        |
| ------------- | ------- | --- | ----------------------------------------- |
| `pattern`     | string  | ✅   | 正则表达式                                     |
| `output_mode` | string  | ❌   | `files_with_matches`/ `content` / `count` |
| `-i`          | boolean | ❌   | 忽略大小写                                     |
| `-C`          | number  | ❌   | 显示上下文 N 行                                 |
>[!搜索函数定义]
>{ "pattern": "function\\s+\\w+", "type": "js", "output_mode": "content" }      

>[!只返回文件列表]
> { "pattern": "TODO", "output_mode": "files_with_matches" }   


### 4.5 💻 Bash - 执行命令

在沙箱环境中执行 shell 命令。

|参数|类型|必需|说明|
|---|---|---|---|
|`command`|string|✅|要执行的命令|
|`timeout`|number|❌|超时（默认 120s，最大 600s）|
|`run_in_background`|boolean|❌|后台运行|

>[!基本命令]
>{ "command": "npm install" }      

>[!长时间运行]
>{ "command": "pytest tests/", "timeout": 300000 } 

>[!后台任务]
>{ "command": "npm run dev", "run_in_background": true }   `

⚠️ **注意**：

- 优先使用专用工具（Read/Glob/Grep）而非 Bash 命令
- 使用 `$TMPDIR` 而非 `/tmp`
- 使用绝对路径避免 `cd`


---

## 五、📁 文件操作工具（完整）

### 5.1 ✍️ Write - 写入文件

|参数|类型|必需|说明|
|---|---|---|---|
|`file_path`|string|✅|文件的绝对路径|
|`content`|string|✅|要写入的内容|

> ⚠️ 覆盖现有文件需要先使用 Read 读取

### 5.2 📓 NotebookEdit - 编辑 Jupyter

| 参数              | 类型     | 必需  | 说明                             |
| --------------- | ------ | --- | ------------------------------ |
| `notebook_path` | string | ✅   | notebook 路径                    |
| `new_source`    | string | ✅   | 新内容                            |
| `edit_mode`     | string | ❌   | `replace`/ `insert` / `delete` |
| `cell_type`     | string | ❌   | `code` / `markdown`            |

---

## 六、⚡ 执行与任务工具

### 6.1 🤖 Task - 启动子 Agent

|参数|类型|必需|说明|
|---|---|---|---|
|`subagent_type`|string|✅|Agent 类型|
|`prompt`|string|✅|任务描述|
|`run_in_background`|boolean|❌|后台运行|

**Agent 类型**：

| 类型                | 用途     |
| ----------------- | ------ |
| `Explore`         | 快速代码探索 |
| `Plan`            | 架构规划   |
| `general-purpose` | 通用任务   |
| `Bash`            | 命令执行专家 |

---

## 七、💬 交互与规划工具

### 7.1 ❓ AskUserQuestion

向用户提问获取选择或输入。

```json
{  
  "questions": [{  
    "question": "选择框架？",  
    "header": "Framework",  
    "options": [  
      { "label": "React", "description": "组件化 UI 库" },  
      { "label": "Vue", "description": "渐进式框架" }  
    ]  
  }]  
}
```

### 7.2 📋 EnterPlanMode / ExitPlanMode

- **EnterPlanMode**：进入规划模式（复杂任务设计阶段）
- **ExitPlanMode**：退出规划模式，请求用户批准

### 7.3 ⚡ Skill

执行用户定义的技能。

```json
{ "skill": "commit" }   
{ "skill": "review-pr", "args": "123" }
```

---

## 八、📋 任务管理工具

|工具|用途|
|---|---|
|`TaskCreate`|创建任务|
|`TaskGet`|获取任务详情|
|`TaskUpdate`|更新任务状态|
|`TaskList`|列出所有任务|

**状态流转**：`pending` → `in_progress` → `completed`

---

## 九、👥 团队协作工具

|工具|用途|
|---|---|
|`TeamCreate`|创建多 Agent 团队|
|`TeamDelete`|删除团队|
|`SendMessage`|向队友发送消息|

**消息类型**：`message` / `broadcast` / `shutdown_request` / `shutdown_response`

---

## 十、🔐 权限配置

### 10.1 权限模式

|模式|图标|安全级|适用场景|
|---|---|---|---|
|`bypassPermissions`|⚠️|最低|CI/CD 自动化|
|`acceptEdits`|🟡|中|个人项目|
|`default`|🟢|高|日常开发|
|`plan`|🔒|最高|生产环境|

### 10.2 配置文件位置

```json
~/.claude/settings.json      # 全局设置  
~/.claude/settings.local.json # 本地覆盖  
.claude/settings.json         # 项目级设置
```

### 10.3 Bash 沙箱限制

默认沙箱配置：

- ✅ 允许读取：所有文件
    
- ✅ 允许写入：`/tmp/claude`、当前目录、标准输出
    
- ❌ 禁止写入：`~/.claude/settings.json`、`~/.ssh/*`、凭证文件
    

---

## 十一、🌐 其他工具

### 11.1 WebSearch - 网络搜索

|参数|类型|必需|说明|
|---|---|---|---|
|`query`|string|✅|搜索查询|
|`allowed_domains`|array|❌|限制域名|

> 仅在美国可用，响应需包含来源

### 11.2 ToolSearch - 工具发现

搜索和加载延迟加载的 MCP 工具。

```json
{ "query": "slack" }  
{ "query": "select:mcp__slack__read_channel" }
```

---

## 十二、💡 最佳实践速查

| 场景   | 推荐          | 避免       |
| ---- | ----------- | -------- |
| 读取文件 | ✅ `Read`    | ❌ `cat`  |
| 编辑代码 | ✅ `Edit`    | ❌ `sed`  |
| 搜索文件 | ✅ `Glob`    | ❌ `find` |
| 搜索内容 | ✅ `Grep`    | ❌ `grep` |
| 临时文件 | ✅ `$TMPDIR` | ❌ `/tmp` |
| 路径指定 | ✅ 绝对路径      | ❌ `cd`   |

---

## 十三、📋 工具速查表

📁 文件操作：Read | Write | Edit | NotebookEdit | Glob | Grep   
⚡ 执行任务：Bash | Task   
💬 交互规划：AskUserQuestion | EnterPlanMode | ExitPlanMode | Skill   
📋 任务管理：TaskCreate | TaskGet | TaskUpdate | TaskList   
👥 团队协作：TeamCreate | TeamDelete | SendMessage   
🌐 其他工具：WebSearch | ToolSearch`







