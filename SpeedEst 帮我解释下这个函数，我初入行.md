这是一个转子转速估计函数,原理是**测周法**——通过测量转速传感器信号相邻下降沿之间的采样周期数,反算出转速频率。

一步拆解:

## 第一段: 模拟信号 → 数字信号(带迟滞)



```c
if( speed_signal < -1000.0 )
    vis_gpio79 = 1;
else if( speed_signal > -800.0 )
    vis_gpio79 = 0;
```

转速传感器输出的是模拟波形(正弦或脉冲),这里用**迟滞比较器**把它变成 0/1 数字信号:

- 低于 -1000 → 判定为 1(脉冲来了)
- 高于 -800 → 判定为 0(脉冲走了)
- 中间地带(-1000 ~ -800)→ 保持不变,防止噪声导致反复跳变



```
传感器信号
    ┃          ╱╲
    ┃        ╱    ╲
-800┃┈┈┈┈┈╱┈┈┈┈┈┈╲┈┈┈┈  ← 上阈值(→0)
    ┃    ╱          ╲
-1000┃┈╱┈┈┈┈┈┈┈┈╲┈  ← 下阈值(→1)
    ┃╱                ╲╱

vis_gpio79:
    ┃  ┌──────┐        ┌──
  1 ┃──┘      └────────┘
  0 ┃
```

## 第二段: 计数



```c
Start = vis_gpio79;
NumUp++;
```

每次 ISR(中断)调用这个函数时,`NumUp` 加 1。ISR 频率是 `adc_fre = 20000 Hz`,所以 `NumUp` 就是在数"两次脉冲之间经过了多少个 50μs"。

## 第三段: 检测下降沿,算频率



```c
if( Start == 0 && PRE == 1 )  // 下降沿: 上一次是1,这次变0
{
    fEst = adc_fre / NumUp;   // 频率 = 采样率 / 周期计数
    Hz = fEst;
    NumUp = 0;                // 重新开始计数
}
```

核心公式: **转速频率 = 20000 / NumUp**

举个例子:

- 转子转一圈,传感器出一个脉冲
- 如果两个下降沿之间数了 400 次 → `fEst = 20000 / 400 = 50 Hz` → 转子转速 3000 RPM

## 第四段: 超时保护



```c
if( NumUp > adc_fre )  // 超过1秒没检测到脉冲
{
    fEst = 0;
    Hz = 0;            // 认为转子停了
}
```

如果 `NumUp > 20000`(即超过 1 秒没有新的下降沿),说明转子已经停转或转速极低,直接置零。

------

总结: 这就是嵌入式里经典的**测周法测频率**,适合低频信号(转速不太高时精度好)。如果转速很高,通常会换成**测频法**(固定时间窗口内数脉冲个数)。